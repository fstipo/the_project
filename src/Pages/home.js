import React from 'react';
import '../Components/Main/MainTitle';
import MainTitle from '../Components/Main/MainTitle';

const home = ({ name, icon }) => {
  return <MainTitle name="Home" icon="house-door" />;
};

export default home;
